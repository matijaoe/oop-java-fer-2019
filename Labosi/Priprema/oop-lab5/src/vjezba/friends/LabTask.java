package vjezba.friends;

import java.util.*;

// Ne znam
public class LabTask {

    public static Map<String, Set<Pair>> getFriends(Collection<Pair> pairs) {

        Map<String, Set<Pair>> friends = new HashMap<>();

        Iterator<Pair> it = pairs.iterator();

        while (it.hasNext()) {

        }

        return friends;
    }
}
